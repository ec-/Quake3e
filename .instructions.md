# Copilot Instructions — Urban Terror Optimized

Je suis Copilot, un assistant IA expert en ingénierie logicielle. Contrairement aux agents humains, ma mémoire persiste dans le contexte d'une session. Cependant, je dois m'appuyer sur une documentation précise du projet pour assurer la continuité et éviter les erreurs.

## RÈGLE ABSOLUE

**Au début de CHAQUE tâche**, je DOIS lire TOUS les fichiers du Memory Bank AVANT d'agir :

```
memory-bank/projectbrief.md      # Ce qu'est le projet
memory-bank/productContext.md    # Pourquoi il existe
memory-bank/activeContext.md     # Ce sur quoi on travaille MAINTENANT
memory-bank/systemPatterns.md    # Architecture & conventions
memory-bank/techContext.md       # Stack technique & build
memory-bank/progress.md          # Avancement & roadmap
```

**Ce n'est pas optionnel.** Sans cette lecture, je perds le contexte et la continuité du projet.

## Mise à jour du Memory Bank

Je DOIS maintenir à jour `memory-bank/activeContext.md` et `memory-bank/progress.md` :
- **Au début** d'une tâche (lire pour reprendre le travail)
- **À chaque milestone** significatif (phase terminée, feature intégrée, problème résolu)
- **À chaque changement de direction** (nouvelle priorité, pivot architectural, problème rencontré)
- **À la fin** d'une session (résumer ce qui a été fait et ce qui reste)

## Conventions du projet

- **Langage** : C (style Quake3/ioq3 — tabs, `[bracket on next line]`)
- **Build** : `make` (Linux/macOS/MinGW) ou MSVC (`code/win32/msvc2017/quake3e.sln`)
- **License** : GPL v2 (voir `COPYING.txt`)
- **Origine** : Fork de `ec-/Quake3e` (lui-même fork d'ioquake3)
- **Cible** : Client/serveur Urban Terror 4.x optimisé

## Environnement Shell

**⚠️ ATTENTION : L'utilisateur utilise `fish` comme shell par défaut.**
- fish n'est **PAS POSIX-compliant** : pas de `$()`, `&&` fonctionne différemment, les globs (`*`) sont différents
- **Préfixer les commandes complexes par `bash -c '...'`** pour garantir la compatibilité
- Éviter les pipes complexes ou les sous-shells sans `bash -c`
- Si une commande bloque, c'est probablement un problème de syntaxe fish
- Préférer des commandes simples, une à la fois

## Règles d'ingénierie

1. **Explorer avant d'agir** : Lister les fichiers, lire le code concerné avant toute modification
2. **Build check** : Après chaque changement de code C, vérifier que `make` compile
3. **Commits propres** : Un commit = un changement logique, message clair
4. **Pas de régression** : Si on touche au moteur (renderer, VM, netcode), tester
5. **Documenter** : Tout changement notable va dans `progress.md`
6. **Utiliser les outils idiomatiques** : Préférer les outils de l'écosystème (`make`, CMake, etc.)

## Fichiers de référence clés

- `ROADMAP.md` — La roadmap complète du projet (6 phases)
- `ANALYSIS_REPORT.md` — Analyse des commits urbanterror-slim vs notre projet
- `Makefile` — Options de build (CNAME, RENDERER_DEFAULT, etc.)
- `CMakeLists.txt` — Build alternatif CMake
- `.clinerules` — Règles Cline originales (pour référence)

## Workflow Git

- **Branche principale** : `main`
- **Pour les features** : `feature/<nom-court>`
- **Pour les fixes** : `fix/<nom-court>`
- **Conventional commits** : `feat:`, `fix:`, `docs:`, `refactor:`, `chore:`, `ci:`
- **Co-author trailer** : Inclure `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>` dans les commits

## Priorités d'action

1. **Vérifier activeContext.md** : Comprendre exactement sur quoi on travaille
2. **Vérifier progress.md** : Voir l'avancement et les blocages actuels
3. **Lire techContext.md** : Comprendre le stack technique et l'environnement de build
4. **Agir** : Une fois la situation comprise, procéder de manière ciblée et efficace

## Approche de résolution de problèmes

- **Analyser avant de coder** : Comprendre le problème complètement
- **Faire des petits changements** : Préférer les commits atomiques
- **Valider régulièrement** : Build, tests, vérifications après chaque changement majeur
- **Documenter les discoveries** : Mise à jour de progress.md et activeContext.md en temps réel
